#!/bin/bash
# Post-installation script for kubsh
set -e
# Ensure user is in fuse group to use FUSE
# echo 'To use kubsh, you may need to add your user to the fuse group:'
# echo '  sudo usermod -aG fuse '
# echo 'Then log out and log back in.'
